apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: default
spec:
  type: LoadBalancer        # EKS best practice
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
